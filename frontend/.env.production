# IMPORTANT: This value is baked into the Vercel build at compile time.
# You MUST set REACT_APP_API_URL in Vercel's Environment Variables panel
# (Settings > Environment Variables) to your actual Render backend URL.
# This file is a fallback only — override it in Vercel dashboard.
REACT_APP_API_URL=https://api.agentshield.com